# 🧪 Testing Guide - Batch Upload UI

## 🌐 Preview Deployment

**URL:** https://educational-analyzer-7kevgyzm8-vladkiaune-gmailcoms-projects.vercel.app

**Branch:** `feature/batch-upload-ui`

---

## ⚠️ **Важные предварительные условия:**

### 1. **Пользователь должен быть авторизован**

Зайдите через `/login` или `/signup`. 

**Без авторизации** страница `/programs` редиректнет на `/login`.

### 2. **База данных**

Миграция `20250122_programs_batch_analyzer.sql` **должна быть применена** на production БД.

Проверить можно:
```sql
SELECT table_name FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name IN ('programs', 'program_lessons', 'program_runs', 'analysis_jobs');
```

Должны существовать все 4 таблицы.

### 3. **Для Yonote источников**

Нужен **credential** (session cookie) в таблице `external_credentials`.

**Пока нет UI для добавления credentials**, поэтому:

#### Вариант A: Использовать Generic List (проще для тестов)

Создайте программу с типом "Список URL" и укажите ссылку на .txt файл со списком:
```
https://example.com/lessons.txt
```

Формат файла:
```
Урок 1 - https://example.com/lesson1
Урок 2 - https://example.com/lesson2
Урок 3 - https://example.com/lesson3
```

#### Вариант B: Добавить Yonote credential вручную в БД

```sql
-- 1. Получить user_id текущего пользователя
SELECT id FROM auth.users WHERE email = 'your-email@example.com';

-- 2. Зашифровать cookie (нужен APP_SECRET_KEY)
-- Это сложно сделать вручную, лучше использовать Generic List для тестов

-- 3. Вставить credential
INSERT INTO external_credentials (user_id, provider, name, cookie_encrypted)
VALUES ('user-uuid', 'yonote', 'My Yonote', 'encrypted-cookie-here');
```

---

## 🎯 **Сценарий тестирования (рекомендуемый):**

### **Шаг 1: Создание программы**

1. Откройте **https://educational-analyzer-7kevgyzm8-vladkiaune-gmailcoms-projects.vercel.app/programs**
2. Если не авторизованы → зарегистрируйтесь/войдите
3. Нажмите **"Добавить новую программу"**
4. Заполните форму:
   - **Название:** "Тест Generic List"
   - **Тип источника:** "Список URL"
   - **URL:** `https://raw.githubusercontent.com/yourusername/lessons/main/list.txt` *(или любой публичный .txt)*
5. Нажмите **"Создать"**

**Ожидаемый результат:**
- ✅ Программа появилась в списке слева
- ✅ Автоматически выбрана
- ✅ Справа показывается заголовок программы
- ✅ Сообщение "Уроки еще не загружены"
- ✅ Кнопка "Загрузить уроки" активна

---

### **Шаг 2: Загрузка списка уроков (enumerate)**

1. Нажмите кнопку **"Загрузить уроки"** в карточке программы
2. Дождитесь завершения

**Ожидаемый результат:**
- ✅ Alert с сообщением "Загружено N уроков"
- ✅ Список уроков отображается справа
- ✅ Кнопка "Запустить" появилась
- ✅ Кнопка "Загрузить уроки" исчезла

**Возможные ошибки:**
- ❌ "Failed to enumerate lessons" → проверьте URL файла (должен быть доступен публично)
- ❌ "No adapter found" → проверьте тип источника

---

### **Шаг 3: Запуск пачного анализа**

1. Нажмите кнопку **"Запустить"**
2. Дождитесь создания run

**Ожидаемый результат:**
- ✅ **ProgressTracker** появился над списком уроков
- ✅ Показывается:
  - Статус: "Выполняется"
  - Прогресс-бар
  - Счетчики: Всего / Обработано / Успешно / Ошибки
  - Кнопки: Пауза / Остановить
- ✅ Каждые 2 секунды автообновление
- ✅ Внизу надпись "Автоматическое обновление каждые 2 секунды"

**Возможные проблемы:**
- ⚠️ **Worker не запущен** → задачи останутся в очереди, прогресс = 0
- ⚠️ **Нет API ключей** → задачи будут падать с ошибками

---

### **Шаг 4: Управление run (опционально)**

Пока run выполняется:

1. Нажмите **"Пауза"**
   - ✅ Статус → "Приостановлен"
   - ✅ Кнопка "Продолжить" появилась
   - ✅ Обработка остановлена

2. Нажмите **"Продолжить"**
   - ✅ Статус → "Выполняется"
   - ✅ Обработка возобновилась

3. Нажмите **"Остановить"**
   - ✅ Подтверждение
   - ✅ Статус → "Остановлен"
   - ✅ ProgressTracker исчезает

---

### **Шаг 5: Просмотр результатов (после завершения)**

Когда run завершится:

**Ожидаемый результат:**
- ✅ Статус → "Завершен"
- ✅ Прогресс = 100%
- ✅ ProgressTracker автоматически скрывается
- ✅ Счетчик "Успешно" обновился
- ✅ Список программ перезагрузился

**TODO (не реализовано):**
- ❌ Статусы уроков в списке (пока всегда "not-started")
- ❌ Клик на урок → просмотр результатов анализа
- ❌ Ссылка на analysis результаты

---

## 🐛 **Известные ограничения:**

### 1. **Worker не запущен**

Для фактической обработки задач нужен **worker process**, который:
- Берет задачи из `analysis_jobs`
- Обрабатывает через LLM
- Сохраняет в `analyses`

**Без worker:**
- ✅ Run создастся
- ✅ Jobs добавятся в очередь
- ❌ Обработка не начнется
- Прогресс останется 0%

**Решение:**
- Запустить worker локально или на Vercel через cron
- Endpoint: `POST /api/worker/tick` или `POST /api/program-runs/{id}/tick`

### 2. **Credentials для Yonote**

Пока **нет UI** для добавления Yonote session cookies.

**Workaround:**
- Используйте Generic List для тестов
- Или добавьте credential вручную в БД

### 3. **Статусы уроков**

Пока все уроки показываются как `not-started`, даже если анализ завершен.

**TODO:**
- Добавить запрос к `analyses` таблице
- Связать по `lesson_id`
- Показывать реальный статус

---

## ✅ **Что работает отлично:**

1. ✅ **Создание программы** - форма, валидация, сохранение в БД
2. ✅ **Enumerate lessons** - загрузка из Generic List или Yonote
3. ✅ **Создание run** - формирование очереди задач
4. ✅ **ProgressTracker** - real-time отслеживание с polling
5. ✅ **Pause/Resume/Stop** - управление выполнением
6. ✅ **UI/UX** - loading states, error handling, auto-refresh

---

## 📝 **Следующие шаги:**

### Высокий приоритет:

1. **Worker** - запустить обработку очереди
   - Локально: `node scripts/run-worker.js`
   - Production: Vercel cron job

2. **Credential Manager** - UI для добавления Yonote cookies
   - Страница `/settings/credentials`
   - Форма с шифрованием

3. **Lesson Status** - связать с analyses
   - Запрос статусов при загрузке lessons
   - Обновление после завершения run

### Средний приоритет:

4. **StartAnalysisModal** - параметры запуска
   - Выбор метрик (lx / custom)
   - Настройка параллелизма

5. **Results View** - просмотр результатов
   - Клик на урок → детали анализа
   - История runs

---

## 🎬 **Быстрый тест (без worker):**

Если нет времени настраивать worker, можно протестировать только UI:

1. Создать программу ✅
2. Загрузить уроки ✅
3. Запустить анализ ✅
4. Увидеть ProgressTracker ✅
5. Попробовать Pause/Resume ✅
6. Остановить run ✅

**Все это работает без worker!**

Только фактическая обработка уроков требует worker.

---

## 🔗 **Полезные ссылки:**

- **Preview:** https://educational-analyzer-7kevgyzm8-vladkiaune-gmailcoms-projects.vercel.app
- **Programs Page:** /programs
- **Документация:** BATCH_UPLOAD_UI.md
- **GitHub Branch:** feature/batch-upload-ui
- **Create PR:** https://github.com/2vlad/educational-analyzer/pull/new/feature/batch-upload-ui

---

**Готово к тестированию! 🚀**
