# Programs Cards View - TODO List

## ✅ Готово:

### 1. Новый дизайн с карточками
- ✅ Компонент `ProgramCard` с collapsed/expanded states
- ✅ Компонент `ProgramsCardsView` для отображения списка
- ✅ Страница `/programs-cards` с новым дизайном
- ✅ Круговой прогресс индикатор
- ✅ Чередование цветов (green/beige)
- ✅ Плавная анимация раскрытия/скрытия

### 2. API для метрик
- ✅ Endpoint `GET /api/programs/[id]/metrics-summary`
  - Агрегирует метрики всех analyzed уроков
  - Вычисляет средний score
  - Округляет до -1, 0, 1, 2
  - Возвращает top comment для каждой метрики
- ✅ Метод `getProgramMetricsSummary()` в apiService

### 3. Интеграция реальных данных
- ✅ ProgramsCardsView загружает реальные метрики через API
- ✅ Loading state пока метрики загружаются
- ✅ Placeholder "Нет данных анализа" если analyses отсутствуют
- ✅ Error handling при загрузке метрик

### 4. Commits
```
20f4c08 - Add new card-based design for programs page
2ad4ef3 - Integrate real metrics from API into program cards
```

## ⚠️ TODO (по приоритету):

### Высокий приоритет:

#### 1. Добавить кнопки действий в карточки
**Где:** В expanded состоянии, внизу карточки

**Нужны кнопки:**
- 📁 **"Загрузить файлы"** - для sourceType='manual'
  - Открывает модал UploadLessonsButton
  - Показывать только если lessonsCount === 0
  
- ▶️ **"Начать анализ"** - если есть уроки
  - Вызывает apiService.createRun()
  - Показывать только если lessonsCount > 0 && status !== 'active'
  
- 🗑️ **"Удалить программу"**
  - Показывает confirm dialog
  - Вызывает apiService.deleteProgram()
  - Удаляет карточку из списка

**Props которые нужно добавить:**
```typescript
interface ProgramCardProps {
  // ... existing props
  programId: string
  sourceType: 'manual' | 'yonote' | 'generic_list'
  status: 'draft' | 'active' | 'completed'
  onUploadFiles?: (programId: string) => void
  onStartAnalysis?: (programId: string) => void
  onDelete?: (programId: string) => void
}
```

**Файлы для изменения:**
- `components/programs/ProgramCard.tsx`
- `components/programs/ProgramsCardsView.tsx`
- `app/programs-cards/page.tsx`

#### 2. Progress Tracker для активных runs
**Где:** Над карточкой программы или вместо метрик

**Варианты:**
- A) Показывать ProgressTracker компонент над карточкой
- B) Заменить метрики на прогресс-бар если status === 'active'
- C) Добавить индикатор загрузки на круговой прогресс

**Рекомендация:** Вариант A - отдельный компонент над карточкой

**Файлы:**
- `components/programs/ProgramsCardsView.tsx` - добавить ProgressTracker
- Переиспользовать существующий `components/programs/ProgressTracker.tsx`

#### 3. Просмотр отдельных уроков
**Где:** В expanded состоянии или по клику на прогресс

**Варианты:**
- A) Ссылка "Показать уроки" → переход на `/programs/:id/lessons`
- B) Мини-список уроков прямо в expanded state
- C) Отдельный modal с уроками

**Рекомендация:** Вариант A - ссылка на отдельную страницу

**Файлы:**
- `components/programs/ProgramCard.tsx` - добавить ссылку
- Возможно создать `app/programs-cards/[id]/lessons/page.tsx`
- Или переиспользовать существующий `app/programs/[programId]/lessons/page.tsx`

### Средний приоритет:

#### 4. Автообновление метрик после анализа
**Проблема:** После завершения анализа метрики не обновляются автоматически

**Решение:**
- Слушать события от ProgressTracker
- При onComplete вызывать refresh метрик
- Или polling каждые N секунд если есть active run

**Файлы:**
- `components/programs/ProgramsCardsView.tsx`
- `app/programs-cards/page.tsx`

#### 5. Skeleton loader вместо "Загрузка метрик..."
**Текущее:** Просто текст "Загрузка метрик..."

**Улучшение:** Skeleton UI с плейсхолдерами
```tsx
<div className="flex items-center gap-6 animate-pulse">
  <div className="h-4 bg-gray-300 rounded w-24"></div>
  <div className="h-4 bg-gray-300 rounded w-24"></div>
  <div className="h-4 bg-gray-300 rounded w-24"></div>
</div>
```

**Файлы:**
- `components/programs/ProgramCard.tsx`

#### 6. Оптимизация загрузки метрик
**Текущее:** Загружаем метрики для всех программ сразу

**Проблема:** Если программ много (>10) - много одновременных запросов

**Решение:**
- Lazy loading - загружать только при раскрытии карточки
- Batch loading - группировать запросы
- Cache - сохранять в state и не перезагружать

**Файлы:**
- `components/programs/ProgramsCardsView.tsx`

### Низкий приоритет:

#### 7. Transition анимация при удалении
**Улучшение:** Плавное исчезновение карточки при удалении

**Файлы:**
- `components/programs/ProgramCard.tsx`

#### 8. Клавиатурная навигация
**Accessibility:** 
- Enter/Space для раскрытия
- Tab navigation
- aria-labels

**Файлы:**
- `components/programs/ProgramCard.tsx`

#### 9. Виртуализация списка
**Когда:** Если программ >50

**Решение:** react-window или react-virtualized

**Файлы:**
- `components/programs/ProgramsCardsView.tsx`

## 🔄 Миграция с /programs на /programs-cards

### Вариант 1: Постепенная
1. Оставить `/programs` как есть
2. Развивать `/programs-cards` параллельно
3. Добавить toggle переключения в настройках
4. После стабилизации заменить `/programs` → редирект на `/programs-cards`

### Вариант 2: Быстрая замена
1. Доделать всю функциональность в `/programs-cards`
2. Переименовать `/programs` → `/programs-old`
3. Переименовать `/programs-cards` → `/programs`
4. Удалить старую версию через неделю

**Рекомендация:** Вариант 1 - постепенная миграция

## 📋 Следующие шаги (по порядку):

1. ✅ ~~Создать API для метрик~~
2. ✅ ~~Интегрировать реальные данные~~
3. **Добавить кнопки действий** ← ВЫ ЗДЕСЬ
4. **Добавить ProgressTracker**
5. **Добавить просмотр уроков**
6. **Тестирование полного флоу**
7. **Оптимизация и UX улучшения**
8. **Миграция с /programs**

## 🧪 Тестирование

### Сценарий 1: Создание программы и анализ
1. Открыть `/programs-cards`
2. Создать новую manual программу
3. Загрузить файлы
4. Начать анализ
5. Дождаться завершения
6. Проверить что метрики отобразились

### Сценарий 2: Программа без анализов
1. Создать программу
2. Загрузить файлы
3. Проверить что показывается "Нет данных анализа"

### Сценарий 3: Удаление программы
1. Удалить программу
2. Проверить что карточка исчезла

## 📊 Метрики успеха

- ✅ Карточки отображаются корректно
- ✅ Реальные метрики загружаются
- ⚠️ Loading states работают
- ❌ Действия с программой (загрузка, анализ, удаление)
- ❌ ProgressTracker показывается для активных runs
- ❌ Просмотр уроков работает

---

**Статус:** 🟡 В разработке (60% готово)

**Следующий шаг:** Добавить кнопки действий в ProgramCard
