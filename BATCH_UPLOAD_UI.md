# Batch Upload UI - Feature Branch

## 📋 Обзор

Эта ветка (`feature/batch-upload-ui`) содержит полноценную реализацию UI для пачной загрузки и анализа уроков.

## ✅ Что реализовано

### 1. **API Service** (`src/services/api.ts`)

Добавлены методы для работы с programs:

```typescript
- getPrograms()              // Получить список программ
- createProgram()            // Создать программу
- deleteProgram()            // Удалить программу
- enumerateLessons()         // Загрузить список уроков из источника
- getProgramLessons()        // Получить уроки программы
- createRun()                // Запустить пачный анализ
- getRunStatus()             // Получить статус выполнения
- pauseRun(), resumeRun(), stopRun()  // Управление запуском
- pollRunProgress()          // Отслеживание прогресса в реальном времени
```

### 2. **API Endpoint**

- `GET /api/programs/[id]/lessons` - получение списка уроков программы

### 3. **UI Компоненты**

#### `AddProgramModal` (обновлен)
- Поле "Название программы"
- Выбор типа источника (Yonote / Список URL)
- Поле "URL источника"
- Валидация формы
- Loading state

#### `/programs` Page (переделан)
- Загрузка реальных данных из API
- Автоматический выбор первой программы
- Обработка ошибок и состояния загрузки
- Редирект на `/login` при отсутствии авторизации

#### `ProgramsList` (обновлен)
- Кнопка "Загрузить уроки" (enumerate) для программ без уроков
- Кнопка "Запустить" для программ с уроками
- Кнопка "Удалить" программу
- Визуализация прогресса выполнения

#### `ProgramLessons` (обновлен)
- Loading state при загрузке уроков
- Сообщение когда уроки не загружены
- Список уроков с сортировкой

## 🎯 Основной Flow

```
1. Создать программу
   ↓
2. Загрузить уроки (enumerate)
   ↓
3. Запустить анализ (create run)
   ↓
4. Отслеживать прогресс (poll status)
   ↓
5. Просмотреть результаты
```

## 🚧 Что осталось доделать

### Высокий приоритет:

1. **StartAnalysisModal** - модальное окно с параметрами запуска:
   - Выбор режима метрик (lx / custom)
   - Выбор конфигурации метрик (для custom mode)
   - Настройка параллелизма (1-10)

2. **ProgressTracker** - компонент отслеживания прогресса:
   - Отображение текущего статуса run
   - Прогресс-бар с процентами
   - Счетчики: всего / обработано / успешно / ошибок
   - Кнопки pause/resume/stop
   - Автоматическое обновление (polling)

3. **Интеграция результатов** - связь анализов с уроками:
   - Показывать статус каждого урока (completed / error / in-progress / not-started)
   - Переход к просмотру результатов анализа урока
   - Повторный анализ конкретного урока

### Средний приоритет:

4. **CredentialSelector** - управление credentials:
   - Выбор существующего Yonote credential
   - Добавление нового credential
   - Проверка валидности cookie

5. **Управление run**:
   - Кнопки pause/resume/stop в UI
   - Подтверждение остановки
   - Отображение статуса run в списке программ

### Низкий приоритет:

6. **Страница управления credentials** (`/settings/credentials`):
   - Список сохраненных credentials
   - Добавление/редактирование/удаление
   - Проверка работоспособности

## 📊 Типы данных

### Program
```typescript
{
  id: string
  name: string
  source_type: 'yonote' | 'generic_list'
  root_url: string
  lastRun?: {
    status: 'queued' | 'running' | 'paused' | 'stopped' | 'completed' | 'failed'
    progress: number  // 0-100
    totalLessons: number
    succeeded: number
    failed: number
  }
}
```

### ProgramLesson
```typescript
{
  id: string
  program_id: string
  title: string
  source_url: string
  sort_order: number
  content_hash?: string
}
```

### ProgramRun
```typescript
{
  id: string
  status: 'queued' | 'running' | 'paused' | 'stopped' | 'completed' | 'failed'
  metrics_mode: 'lx' | 'custom'
  total_lessons: number
  processed: number
  succeeded: number
  failed: number
  max_concurrency: number
}
```

## 🧪 Тестирование

### Базовый сценарий:

1. **Зайти на страницу** `/programs`
   - Должен показаться список программ (или пустое состояние)

2. **Создать программу**:
   - Нажать "Добавить новую программу"
   - Заполнить поля:
     * Название: "Тестовая программа"
     * Тип: Yonote
     * URL: `https://practicum.yandex.ru/trainer/...`
   - Создать

3. **Загрузить уроки**:
   - Выбрать программу в списке
   - Нажать "Загрузить уроки"
   - Дождаться завершения (alert с количеством)

4. **Запустить анализ**:
   - Нажать "Запустить"
   - Дождаться создания run (alert)

5. **Отслеживать прогресс**:
   - TODO: Добавить ProgressTracker

### Требования:

- Пользователь должен быть **авторизован**
- Для Yonote нужен **credential** (session cookie)

## 🔑 Переменные окружения

Все переменные из основного README + миграция базы данных должна быть применена:

```bash
# Применить миграцию (если еще не применена)
psql $DATABASE_URL < migrations/20250122_programs_batch_analyzer.sql
```

## 📝 Следующие шаги

1. Создать `StartAnalysisModal` компонент
2. Создать `ProgressTracker` компонент
3. Связать analyses с lessons для отображения статусов
4. Добавить worker для обработки очереди (может быть отдельный PR)
5. Протестировать полный flow
6. Создать PR в main

## 🐛 Известные ограничения

1. **Статусы уроков** - пока всегда 'not-started', нужно получать из analyses
2. **Credentials** - пока нет UI для управления, нужно добавлять вручную в БД
3. **Progress tracking** - нет автоматического обновления, нужен polling
4. **Worker** - пачная обработка требует запущенный worker (cron или отдельный сервис)

## 🎨 UI/UX улучшения (опционально)

- Toast notifications вместо alert()
- Анимации переходов между состояниями
- Drag-and-drop для reorder уроков
- Bulk actions (выбрать несколько уроков для анализа)
- Экспорт результатов в CSV/PDF
- Фильтры и поиск по урокам
