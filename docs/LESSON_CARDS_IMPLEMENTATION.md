# Lesson Cards Implementation - 2025-11-12

## ✅ Реализовано

### 1. LessonCard компонент (`components/lessons/LessonCard.tsx`)

Карточка для отображения одного урока с метриками анализа.

**Features:**
- **Collapsed state (свернутая):**
  - Название урока
  - Drag handle (иконка для перетаскивания)
  - Метрики в одну строку: "Интерес +1, Логика +1, Забота 0, Понятность +2"
  - Или "Не проанализировано" если нет анализа
  - Стрелка раскрытия (ChevronRight)

- **Expanded state (развернутая):**
  - Название урока
  - Детальные метрики:
    - Название метрики + оценка
    - Комментарий/рекомендация
  - Кнопки действий:
    - 🎬 "Анализировать" (если еще не анализировался)
    - 🗑️ "Удалить"
  - Стрелка скрытия (ChevronDown)

**Props:**
```typescript
interface LessonCardProps {
  title: string                    // Название урока
  metrics: LessonMetric[]          // Метрики с оценками и комментариями
  analyzed: boolean                // Есть ли анализ
  color?: 'green' | 'beige'       // Цвет карточки
  loading?: boolean                // Загрузка метрик
  lessonId?: string                // ID урока
  onAnalyze?: (id: string) => void // Запустить анализ
  onDelete?: (id: string) => void  // Удалить урок
  draggable?: boolean              // Показывать drag handle
}
```

**Дизайн:**
- Округлённые углы: `rounded-3xl`
- Цвета: `bg-green-100` или `bg-amber-50` (чередуются)
- Hover эффект: `opacity-80`
- Drag handle слева (только если `draggable=true`)

### 2. Program Lessons Page (`app/programs/[programId]/lessons/page.tsx`)

Страница со списком всех уроков конкретной программы.

**Features:**
- Заголовок с названием программы (uppercase)
- Кнопка "Назад" → `/programs`
- Кнопка "Загрузить ещё уроки" (TODO: функциональность)
- Список уроков в виде карточек
- Автоматическая загрузка метрик для каждого урока

**Как работает:**
1. Загружает программу из API
2. Загружает все уроки программы
3. Для каждого урока делает запрос на `/api/programs/[id]/lessons/[lessonId]/analysis`
4. Если анализ найден - извлекает метрики и показывает
5. Если не найден - показывает "Не проанализировано"

**Пример URL:**
```
http://localhost:3002/programs/1357d9fc-bbd8-4b68-af2b-a14d79278ef1/lessons
```

### 3. API Endpoint для анализа урока

**GET `/api/programs/[id]/lessons/[lessonId]/analysis`**

Возвращает последний completed analysis для урока.

**Response (success):**
```json
{
  "analysis": {
    "id": "...",
    "lesson_id": "...",
    "status": "completed",
    "results": {
      "Интерес": {
        "score": 1,
        "comment": "Обычный текст, без души...",
        "examples": [...]
      },
      "Логика": { ... },
      ...
    },
    "created_at": "...",
    "model_used": "..."
  }
}
```

**Response (404):**
```json
{
  "analysis": null
}
```

**Security:**
- Проверяет что пользователь владеет программой
- Проверяет что урок принадлежит программе
- Только для авторизованных пользователей

### 4. API Endpoint для урока

**GET `/api/programs/[id]/lessons/[lessonId]`**

Возвращает детали конкретного урока (заглушка, будет использоваться позже).

## 📸 Скриншоты

### Collapsed state (список)
![Lesson cards collapsed](../screenshots/lesson-cards-collapsed.png)

- Уроки показаны списком
- Название + метрики в строку
- Чередующиеся цвета
- Drag handle слева

### Expanded state (детали)
![Lesson card expanded](../screenshots/lesson-card-expanded.png)

- Детальные комментарии по метрикам
- Кнопки "Анализировать" и "Удалить"
- Все метрики с пояснениями

## 🎨 Дизайн

### Чередование цветов

```typescript
const colors: ('green' | 'beige')[] = ['green', 'beige', 'green']
// Урок 0: green
// Урок 1: beige  
// Урок 2: green
// Урок 3: beige
// ...
```

### Формат оценок

```typescript
const formatScore = (score: number) => {
  if (score > 0) return `+${score}`  // +1, +2
  return score.toString()             // 0, -1
}
```

### Состояния

1. **Loading:** "Загрузка метрик..."
2. **No analysis:** "Не проанализировано"
3. **Analyzed:** Метрики с оценками

## 🚧 TODO

### Высокий приоритет:

#### 1. Drag-and-drop для изменения порядка уроков

**Библиотека:** `@dnd-kit/core` или `react-beautiful-dnd`

**Что нужно:**
- Установить библиотеку
- Обернуть список в DragDropContext
- Добавить обработчик onDragEnd
- Вызывать API для сохранения нового порядка

**API endpoint (нужно создать):**
```typescript
PATCH /api/programs/[id]/lessons/reorder
Body: { lessonIds: string[] }  // Новый порядок
```

**Пример:**
```typescript
import { DndContext, closestCenter } from '@dnd-kit/core';
import { arrayMove, SortableContext, verticalListSortingStrategy } from '@dnd-kit/sortable';

function handleDragEnd(event) {
  const { active, over } = event;
  if (active.id !== over.id) {
    setLessons((items) => {
      const oldIndex = items.findIndex(i => i.id === active.id);
      const newIndex = items.findIndex(i => i.id === over.id);
      return arrayMove(items, oldIndex, newIndex);
    });
    
    // Save to API
    await apiService.reorderLessons(programId, newLessonIds);
  }
}
```

#### 2. Подключить кнопку "Анализировать"

**Сейчас:** Только console.log

**Нужно:**
- Создать API endpoint для анализа одного урока
- Показать loading state на карточке
- После завершения - перезагрузить метрики

**Варианты:**

**A) Простой (рекомендуется):**
```typescript
const handleAnalyzeLesson = async (lessonId: string) => {
  // Перенаправить на главную страницу с предзаполненным контентом
  const lesson = lessons.find(l => l.id === lessonId);
  router.push(`/?lessonId=${lessonId}`);
}
```

**B) Сложный:**
Создать отдельную страницу `/programs/[id]/lessons/[lessonId]/analyze`

#### 3. Подключить кнопку "Загрузить ещё уроки"

**Нужно:**
- Открывать UploadLessonsButton modal
- После загрузки - обновить список уроков
- Возможно, автоматически запустить анализ новых уроков

**Пример:**
```typescript
const [showUploadModal, setShowUploadModal] = useState(false);

<Button onClick={() => setShowUploadModal(true)}>
  Загрузить ещё уроки
</Button>

{showUploadModal && (
  <UploadLessonsButton
    programId={programId}
    programName={programTitle}
    onSuccess={() => {
      setShowUploadModal(false);
      loadProgramAndLessons(); // Reload
    }}
  />
)}
```

### Средний приоритет:

#### 4. Автообновление после анализа

**Проблема:** Если запустить анализ, метрики не обновятся автоматически

**Решение:**
- Polling каждые N секунд если есть активный run
- Или WebSocket для real-time updates
- Или вручную обновлять через кнопку

#### 5. Skeleton loader для метрик

**Сейчас:** "Загрузка метрик..."

**Улучшение:** Skeleton UI
```tsx
<div className="flex items-center gap-6 animate-pulse">
  <div className="h-4 bg-gray-300 rounded w-24"></div>
  <div className="h-4 bg-gray-300 rounded w-24"></div>
  <div className="h-4 bg-gray-300 rounded w-24"></div>
</div>
```

#### 6. Фильтры и сортировка

**Features:**
- Фильтр: показать только проанализированные / не проанализированные
- Сортировка: по названию, по дате, по средней оценке
- Поиск по названию урока

### Низкий приоритет:

#### 7. Bulk actions (массовые операции)

**Features:**
- Checkbox на каждой карточке
- "Выбрать все"
- "Удалить выбранные"
- "Анализировать выбранные"

#### 8. Экспорт результатов

**Features:**
- Экспорт в CSV
- Экспорт в PDF отчет
- Экспорт в JSON

## 🧪 Testing

### Manual test scenarios:

#### Scenario 1: Просмотр уроков программы
1. Открыть `/programs`
2. Кликнуть на программу
3. Должна открыться страница с уроками
4. Уроки отображаются в виде карточек
5. Если есть анализ - показываются метрики
6. Если нет - "Не проанализировано"

#### Scenario 2: Раскрытие карточки
1. Кликнуть на карточку урока
2. Карточка раскрывается
3. Показываются детальные комментарии
4. Показываются кнопки действий

#### Scenario 3: Удаление урока
1. Раскрыть карточку
2. Кликнуть "Удалить"
3. Подтвердить в confirm dialog
4. Карточка исчезает из списка

## 📊 Метрики успеха

- ✅ Карточки уроков отображаются
- ✅ Метрики загружаются из API
- ✅ Раскрытие/скрытие работает
- ✅ Кнопка удаления работает
- ❌ Drag-and-drop
- ❌ Кнопка "Анализировать"
- ❌ Кнопка "Загрузить ещё уроки"

---

**Статус:** 🟡 60% готово

**Коммит:** `d3d9890` - "Add lesson cards view for individual program page"

**Tested:** ✅ Локально работает!

**Next steps:** 
1. Drag-and-drop для изменения порядка
2. Подключить кнопку "Анализировать"
3. Подключить загрузку файлов
